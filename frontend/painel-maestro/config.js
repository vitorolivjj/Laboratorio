/** Configuração do Painel Maestro (edite URLs externas aqui) */
window.MAESTRO_CONFIG = {
  /** Board Miro “Mapa de agentes” — deixe vazio para usar mapa local */
  miroBoardUrl: "",
  /** Markdown de métricas operacionais (GitHub Actions) */
  metricsDashboardPath: "/dashboard/metricas_operacionais.md",
  /** Fallback local quando miroBoardUrl vazio */
  agentMapLocalPath: "mapa-agentes.html",
};
